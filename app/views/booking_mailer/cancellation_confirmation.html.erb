<h1>Cancellation Confirmation: <%= @event_instance.event.title %></h1>
<%# <p>Dear <%= @user.name %>
<%# ,</p>  %>
<p>Your booking for <%= @event_instance.event.title %>
  <% time_zone_str = @user&.time_zone.presence || "UTC" %>
  <% time_zone = ActiveSupport::TimeZone[time_zone_str] || ActiveSupport::TimeZone["UTC"] %>
  on <%= @event_instance.start_time.in_time_zone(time_zone).strftime("%Y/%m/%d (%a)") %> at <%= @event_instance.start_time.in_time_zone(time_zone).strftime("%H:%M") %>
  has been cancelled.</p>
<p>Best regards,<br>
  Your App Team</p>
