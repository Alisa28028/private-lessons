<nav class="pl-nav pl-top-nav">
  <% if user_signed_in? %>
    <section class="pl-nav-content">
      <ul class="me-3">
        <li class="pl-nav-item">
          <%= link_to "/" do %>
            <%= inline_svg "house-solid.svg", class: "pl-icon" %>
            <p>Home</p>
          <% end %>
        </li>
        <li class="pl-nav-item">
          <%= link_to "#" do %>
            <%= inline_svg "film-solid.svg", class: "pl-icon" %>
            <p>Posts</p>
          <% end %>
        </li>
        <li class="pl-nav-item">
          <%= link_to "#" do %>
            <%= inline_svg "square-plus-regular.svg", class: "pl-icon" %>
            <p>New Post</p>
          <% end %>
        </li>
        <li class="pl-nav-item">
          <%= link_to search_path, class: "item-drawer-button" do %>
            <%= inline_svg "magnifying-glass-solid.svg", class: "pl-icon" %>
            <p>Search</p>
          <% end %>
        </li>
        <li class="pl-dropdown-menu">
          <a href="#" class="pl-avatar-frame">
            <%# <%= image_tag "profilepic.jpg", class: "pl-avatar" %>
            <% if current_user.photo.attached? %>
              <%= cl_image_tag(current_user.photo.key, class: "pl-avatar") %>
            <% else %>
              <%= image_tag "default-avatar.png", class: "pl-avatar" %>
            <% end %>
          </button>
          <div class="pl-dropdown-content">
            <%= link_to "Profile", user_path(current_user) %>
            <%= link_to "Dashboard", dashboard_path(current_user) %>
            <%= link_to "Log Out", destroy_user_session_path, data: {turbo_method: :delete} %>
          </div>
        </li>
      </ul>
    </section>
  <% else %>
    <header class="header-overlay d-flex justify-content-between mt-3 pb-2">
      <div class="logo">
        <%= link_to root_path do %>
          <%= image_tag "PL_logo.png", class: "pl-logo" %>
        <% end %>
      </div>
      <% unless user_signed_in? %>
        <div class="hamburger">
          <i class="fa-solid fa-bars fa-xl <%= current_page?(root_path) ? 'text-white' : 'text-custom' %>" data-action="click->menu#toggle"></i>
        </div>
      <% end %>
    </header>
  <% end %>
</nav>
